const cluster = require('./helpers')
const assert = require('chai').assert

/* describe('health.metrics', () => {
    it('lists latest metrics logged by this peer', (done) => {
        cluster.health.metrics('name', {}, (err, health) => {
            assert.notExists(err, 'throws error while listing latest metrics logged by this peer')
            done()
        })
    })
}) */